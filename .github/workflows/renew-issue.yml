name: RENEW-ISSUE

on:
  workflow_dispatch:

jobs:
  renew-sealos-issue:
    uses: apecloud/apecloud-cd/.github/workflows/renew-issue.yml@v0.1.16
    with:
      APECD_REF: "v0.1.16"
    secrets: inherit
