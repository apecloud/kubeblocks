---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.9.0
  creationTimestamp: null
  name: clusterfamilies.apps.kubeblocks.io
spec:
  group: apps.kubeblocks.io
  names:
    categories:
    - kubeblocks
    kind: ClusterFamily
    listKind: ClusterFamilyList
    plural: clusterfamilies
    shortNames:
    - cf
    singular: clusterfamily
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ClusterFamily is the Schema for the clusterfamilies API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: ClusterFamilySpec defines the desired state of ClusterFamily
            properties:
              clusterTemplateRefs:
                description: Foo is an example field of ClusterFamily. Edit clusterfamily_types.go
                  to remove/update
                items:
                  properties:
                    expression:
                      type: string
                    key:
                      type: string
                    selector:
                      items:
                        properties:
                          expression:
                            type: string
                          template:
                            type: string
                          value:
                            type: string
                        type: object
                      type: array
                    templateRef:
                      type: string
                    value:
                      type: string
                  type: object
                type: array
            type: object
          status:
            description: ClusterFamilyStatus defines the observed state of ClusterFamily
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
