---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.9.0
  creationTimestamp: null
  name: consensussets.dbaas.infracreate.com
spec:
  group: dbaas.infracreate.com
  names:
    kind: ConsensusSet
    listKind: ConsensusSetList
    plural: consensussets
    singular: consensusset
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ConsensusSet is the Schema for the consensussets API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: ConsensusSetSpec defines the desired state of ConsensusSet
            properties:
              followers:
                description: Followers, has voting right but not Leader
                items:
                  properties:
                    accessMode:
                      default: None
                      description: AccessMode, what service this member capable for
                      enum:
                      - None
                      - Readonly
                      - ReadWrite
                      type: string
                    name:
                      description: Name, role name
                      type: string
                    replicas:
                      default: 0
                      description: Replicas, number of Pods of this role default 1
                        for Leader default 0 for Learner default Components[*].Replicas
                        - Leader.Replicas - Learner.Replicas for Followers
                      format: int32
                      minimum: 0
                      type: integer
                  type: object
                type: array
              leader:
                description: Leader, one single leader
                properties:
                  accessMode:
                    default: None
                    description: AccessMode, what service this member capable for
                    enum:
                    - None
                    - Readonly
                    - ReadWrite
                    type: string
                  name:
                    description: Name, role name
                    type: string
                  replicas:
                    default: 0
                    description: Replicas, number of Pods of this role default 1 for
                      Leader default 0 for Learner default Components[*].Replicas
                      - Leader.Replicas - Learner.Replicas for Followers
                    format: int32
                    minimum: 0
                    type: integer
                type: object
              learner:
                description: Learner, no voting right
                properties:
                  accessMode:
                    default: None
                    description: AccessMode, what service this member capable for
                    enum:
                    - None
                    - Readonly
                    - ReadWrite
                    type: string
                  name:
                    description: Name, role name
                    type: string
                  replicas:
                    default: 0
                    description: Replicas, number of Pods of this role default 1 for
                      Leader default 0 for Learner default Components[*].Replicas
                      - Leader.Replicas - Learner.Replicas for Followers
                    format: int32
                    minimum: 0
                    type: integer
                type: object
              replicas:
                default: 1
                description: Replicas, number of pods in this ConsensusSet
                type: integer
              updateStrategy:
                default: Serial
                description: 'UpdateStrategy, Pods update strategy options: serial,
                  bestEffortParallel, parallel serial: update Pods one by one that
                  guarantee minimum component unavailable time Learner -> Follower(with
                  AccessMode=none) -> Follower(with AccessMode=readonly) -> Follower(with
                  AccessMode=readWrite) -> Leader bestEffortParallel: update Pods
                  in parallel that guarantee minimum component un-writable time Learner,
                  Follower(minority) in parallel -> Follower(majority) -> Leader,
                  keep majority online all the time parallel: force parallel'
                enum:
                - Serial
                - BestEffortParallel
                - Parallel
                type: string
            type: object
          status:
            description: ConsensusSetStatus defines the observed state of ConsensusSet
            properties:
              consensusSetConditions:
                description: ConsensusSetCondition
                items:
                  properties:
                    lastTransitionTime:
                      description: Last time the condition transitioned from one status
                        to another.
                      format: date-time
                      type: string
                    message:
                      description: A human-readable message indicating details about
                        the transition.
                      type: string
                    reason:
                      description: The reason for the condition's last transition.
                      type: string
                    status:
                      description: Status of the condition, one of True, False, Unknown.
                      type: string
                    type:
                      description: Type of consensusset condition.
                      type: string
                  type: object
                type: array
              isReadWriteServiceReady:
                default: false
                description: IsReadWriteServiceReady, indicates readWrite service
                  ready status
                type: boolean
              isReadonlyServiceReady:
                default: false
                description: IsReadonlyServiceReady, indicates readonly service ready
                  status
                type: boolean
              observedGeneration:
                description: ObservedGeneration is the most recent generation observed
                  for this ConsensusSet.
                format: int64
                type: integer
              readyFollowers:
                default: 0
                description: ReadyFollowers, ready follower pods
                format: int32
                type: integer
              readyLeader:
                default: 0
                description: ReadyLeader, ready leader pod, 0 or 1
                format: int32
                type: integer
              readyLearners:
                default: 0
                description: ReadyLearners, ready learner pods
                format: int32
                type: integer
              readyReplicas:
                default: 0
                description: ReadyReplicas is the number of pods created for this
                  ConsensusSet with a Ready Condition.
                format: int32
                type: integer
              replicas:
                default: 0
                description: Replicas is the number of Pods created by the controller
                format: int32
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
