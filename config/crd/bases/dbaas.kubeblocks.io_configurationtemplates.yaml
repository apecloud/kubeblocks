---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.9.0
  creationTimestamp: null
  name: configurationtemplates.dbaas.kubeblocks.io
spec:
  group: dbaas.kubeblocks.io
  names:
    categories:
    - dbaas
    - all
    kind: ConfigurationTemplate
    listKind: ConfigurationTemplateList
    plural: configurationtemplates
    shortNames:
    - ctpl
    singular: configurationtemplate
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: status phase
      jsonPath: .status.phase
      name: PHASE
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ConfigurationTemplate is the Schema for the configurationtemplates
          API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: ConfigurationTemplateSpec defines the desired state of ConfigurationTemplate
            properties:
              cfgSchemaTopLevelName:
                description: CfgSchemaTopLevelName is cue type name, which generates
                  openapi schema.
                type: string
              configurationSchema:
                description: ConfigurationSchema imposes restrictions on database
                  parameter's rule.
                properties:
                  cue:
                    description: Cue that to let provider verify user configuration
                      through cue language.
                    type: string
                  schema:
                    description: Schema provides a way for providers to validate the
                      changed parameters through json.
                    type: object
                    x-kubernetes-preserve-unknown-fields: true
                type: object
              dynamicParameters:
                description: DynamicParameters, list of DynamicParameter, modifications
                  of them trigger a config dynamic reload without process restart.
                items:
                  type: string
                type: array
              formatter:
                default: yaml
                description: Formatter describes the format of the configuration file,
                  the controller 1. parses configuration file 2. analyzes the modified
                  parameters 3. applies corresponding policies.
                enum:
                - dotenv
                - ini
                - yaml
                - json
                - hcl
                type: string
              immutableParameters:
                description: ImmutableParameters describes parameters that prohibit
                  user from modification.
                items:
                  type: string
                type: array
              staticParameters:
                description: StaticParameters, list of StaticParameter, modifications
                  of them trigger a process restart.
                items:
                  type: string
                type: array
            type: object
          status:
            description: ConfigurationTemplateStatus defines the observed state of
              ConfigurationTemplate.
            properties:
              message:
                description: Message field describes the reasons of abnormal status.
                type: string
              observedGeneration:
                description: observedGeneration is the latest generation observed
                  for this ClusterDefinition. It refers to the ConfigurationTemplate's
                  generation, which is updated by the API Server.
                format: int64
                type: integer
              phase:
                description: Phase is status of configuration template, when set to
                  AvailablePhase, it can be referenced by ClusterDefinition or AppVersion.
                enum:
                - Available
                - Unavailable
                - Deleting
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
