---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.9.0
  creationTimestamp: null
  name: agamottoexporters.monitor.kubeblocks.io
spec:
  group: monitor.kubeblocks.io
  names:
    kind: AgamottoExporter
    listKind: AgamottoExporterList
    plural: agamottoexporters
    singular: agamottoexporter
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AgamottoExporter is the Schema for the agamottoexporters API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: AgamottoExporterSpec defines the desired state of AgamottoExporter
            properties:
              logsExporter:
                properties:
                  loki_config:
                    properties:
                      endpoint:
                        type: string
                      queue_config:
                        properties:
                          enabled:
                            description: Enabled indicates whether to not enqueue
                              batches before sending to the consumerSender.
                            type: boolean
                          num_consumers:
                            description: NumConsumers is the number of consumers from
                              the queue.
                            type: integer
                          queue_size:
                            description: QueueSize is the maximum number of batches
                              allowed in queue at a given time.
                            type: integer
                        required:
                        - enabled
                        - num_consumers
                        - queue_size
                        type: object
                      retry_config:
                        properties:
                          enabled:
                            description: Enabled indicates whether to not retry sending
                              batches in case of export failure.
                            type: boolean
                          initial_interval:
                            description: InitialInterval the time to wait after the
                              first failure before retrying.
                            format: int64
                            type: integer
                          max_elapsed_time:
                            description: MaxElapsedTime is the maximum amount of time
                              (including retries) spent trying to send a request/batch.
                              Once this value is reached, the data is discarded.
                            format: int64
                            type: integer
                          max_interval:
                            description: MaxInterval is the upper bound on backoff
                              interval. Once this value is reached the delay between
                              consecutive retries will always be `MaxInterval`.
                            format: int64
                            type: integer
                          multiplier:
                            description: Multiplier is the value multiplied by the
                              backoff interval bounds
                            format: int64
                            type: integer
                          randomization_factor:
                            description: RandomizationFactor is a random factor used
                              to calculate next backoffs Randomized interval = RetryInterval
                              * (1 ± RandomizationFactor)
                            format: int64
                            type: integer
                        required:
                        - enabled
                        - initial_interval
                        - max_elapsed_time
                        - max_interval
                        - multiplier
                        - randomization_factor
                        type: object
                    required:
                    - endpoint
                    - queue_config
                    - retry_config
                    type: object
                  s3_config:
                    properties:
                      credentials:
                        properties:
                          namespace:
                            type: string
                          secret:
                            type: string
                        required:
                        - namespace
                        - secret
                        type: object
                      inline:
                        properties:
                          bucket:
                            type: string
                          file_prefix:
                            type: string
                          partition:
                            type: string
                          prefix:
                            type: string
                          region:
                            description: prefix = metric/logs filePrefix = slow/error/auditlog
                              path metric/year=XXXX/month=XX/day=XX/hour=XX/minute=XX/filePrefix
                            type: string
                        required:
                        - bucket
                        - file_prefix
                        - partition
                        - prefix
                        - region
                        type: object
                    required:
                    - credentials
                    - inline
                    type: object
                required:
                - loki_config
                - s3_config
                type: object
              metricsExporter:
                properties:
                  prometheus:
                    type: object
                required:
                - prometheus
                type: object
            required:
            - logsExporter
            - metricsExporter
            type: object
          status:
            description: AgamottoExporterStatus defines the observed state of AgamottoExporter
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
