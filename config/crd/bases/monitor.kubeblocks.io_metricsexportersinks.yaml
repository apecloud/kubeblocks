apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.12.1
  labels:
    app.kubernetes.io/name: kubeblocks
  name: metricsexportersinks.monitor.kubeblocks.io
spec:
  group: monitor.kubeblocks.io
  names:
    kind: MetricsExporterSink
    listKind: MetricsExporterSinkList
    plural: metricsexportersinks
    singular: metricsexportersink
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: MetricsExporterSink is the Schema for the metricsexportersinks
          API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: MetricsExporterSinkSpec defines the desired state of MetricsExporterSink
            properties:
              prometheusConfig:
                description: prometheusConfig defines the config of the loki
                properties:
                  clusterName:
                    description: Specify the cluster of the referenced service.
                    type: string
                  endpoint:
                    description: TODO using serviceRef to reference the service Specify
                      the endpoint of the referenced service.
                    type: string
                  namespace:
                    default: default
                    description: Specify the namespace of the referenced the serviceConnectionCredential
                      object. An empty namespace is equivalent to the "default" namespace.
                    maxLength: 63
                    pattern: ^[a-z0-9]([a-z0-9\-]*[a-z0-9])?$
                    type: string
                  serviceConnectionCredential:
                    description: Specify the name of the referenced the serviceConnectionCredential
                      object.
                    type: string
                required:
                - endpoint
                type: object
              prometheusRemoteWriteConfig:
                description: prometheusRemoteWriteConfig defines the config of the
                  prometheus remote write
                properties:
                  clusterName:
                    description: Specify the cluster of the referenced service.
                    type: string
                  endpoint:
                    description: TODO using serviceRef to reference the service Specify
                      the endpoint of the referenced service.
                    type: string
                  namespace:
                    default: default
                    description: Specify the namespace of the referenced the serviceConnectionCredential
                      object. An empty namespace is equivalent to the "default" namespace.
                    maxLength: 63
                    pattern: ^[a-z0-9]([a-z0-9\-]*[a-z0-9])?$
                    type: string
                  serviceConnectionCredential:
                    description: Specify the name of the referenced the serviceConnectionCredential
                      object.
                    type: string
                required:
                - endpoint
                type: object
              type:
                description: type defines the type of the exporterSink
                enum:
                - prometheus
                - prometheusremotewrite
                type: string
            required:
            - type
            type: object
          status:
            description: MetricsExporterSinkStatus defines the observed state of MetricsExporterSink
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
