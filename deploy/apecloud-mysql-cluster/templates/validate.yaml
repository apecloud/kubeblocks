{{- if .Values.replicas }}
  {{- if and (eq .Values.mode "raftGroup") (ne (mod (int .Values.replicas) 2) 1) }}
    {{ fail "ApeCloud MySQL cluster does not support running with even number replicas." }}
  {{- end }}
{{- end }}