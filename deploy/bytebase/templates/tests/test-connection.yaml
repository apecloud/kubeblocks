apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bytebase.fullname" . }}-test-connection"
  labels:
    {{- include "bytebase.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bytebase.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
