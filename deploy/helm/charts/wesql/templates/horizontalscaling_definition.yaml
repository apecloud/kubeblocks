apiVersion: dbaas.infracreate.com/v1alpha1
kind: OpsDefinition
metadata:
  name: wesql-horizontalscaling-definition
  labels:
    {{- include "wesqlapp.labels" . | nindent 4 }}
spec:
  clusterDefinitionRef: wesql-clusterdefinition
  type: HorizontalScaling
  strategies:
    components:
    - type: replicasets
