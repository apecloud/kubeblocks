---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.9.0
  creationTimestamp: null
  name: backups.dataprotection.kubeblocks.io
spec:
  group: dataprotection.kubeblocks.io
  names:
    categories:
    - dbaas
    kind: Backup
    listKind: BackupList
    plural: backups
    singular: backup
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: STATUS
      type: string
    - jsonPath: .status.completionTimestamp
      name: COMPLETIONTIME
      type: date
    - jsonPath: .status.totalSize
      name: TOTALSIZE
      type: string
    - jsonPath: .status.duration
      name: DURATION
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Backup is the Schema for the backups API (defined by User)
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: BackupSpec defines the desired state of Backup
            properties:
              backupPolicyName:
                description: which backupPolicy to perform this backup
                pattern: ^[a-z0-9]([a-z0-9\.\-]*[a-z0-9])?$
                type: string
              backupType:
                default: full
                description: Backup Type. full or incremental or snapshot. if unset,
                  default is full.
                enum:
                - full
                - incremental
                - snapshot
                type: string
              parentBackupName:
                description: if backupType is incremental, parentBackupName is required.
                type: string
              ttl:
                description: TTL is a time.Duration-parsable string describing how
                  long the Backup should be retained for.
                type: string
            required:
            - backupPolicyName
            - backupType
            type: object
          status:
            description: BackupStatus defines the observed state of Backup
            properties:
              CheckPoint:
                description: backup check point, for incremental backup.
                type: string
              checkSum:
                description: checksum of backup file, generated by md5 or sha1 or
                  sha256
                type: string
              completionTimestamp:
                description: Date/time when the backup finished being processed.
                format: date-time
                type: string
              duration:
                description: The duration time of backup execution. When converted
                  to a string, the form is "1h2m0.5s".
                type: string
              expiration:
                description: The date and time when the Backup is eligible for garbage
                  collection. 'null' means the Backup is NOT be cleaned except delete
                  manual.
                format: date-time
                type: string
              failureReason:
                description: the reason if backup failed.
                type: string
              parentBackupName:
                description: record parentBackupName if backupType is incremental.
                type: string
              phase:
                description: BackupPhase The current phase. Valid values are New,
                  InProgress, Completed, Failed.
                type: string
              startTimestamp:
                description: Date/time when the backup started being processed.
                format: date-time
                type: string
              totalSize:
                description: backup total size string with capacity units in the form
                  of "1Gi", "1Mi", "1Ki".
                type: string
              uploadTotalSize:
                description: backup total size string with capacity units in the form
                  of "1Gi", "1Mi", "1Ki".
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
