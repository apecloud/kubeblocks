apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.9.0
  creationTimestamp: null
  labels:
    app.kubernetes.io/name: kubeblocks
  name: collectordatasources.monitor.kubeblocks.io
spec:
  group: monitor.kubeblocks.io
  names:
    kind: CollectorDataSource
    listKind: CollectorDataSourceList
    plural: collectordatasources
    singular: collectordatasource
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: CollectorDataSource is the Schema for the collectordatasources
          API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: CollectorDataSourceSpec defines the desired state of CollectorDataSource
            properties:
              collectionInterval:
                description: CollectionInterval is the interval of the data source
                type: string
              dataSourceList:
                description: DataSourceList is the list of the data source
                items:
                  properties:
                    name:
                      description: Name is the name of the data source
                      type: string
                    parameter:
                      description: Parameter is the parameter of the data source
                      type: string
                  required:
                  - name
                  type: object
                type: array
              exporterRef:
                items:
                  type: string
                type: array
              mode:
                description: Mode represents how the OTeld should be deployed (deployment,
                  daemonset, statefulset or sidecar)
                enum:
                - daemonset
                - deployment
                - sidecar
                - statefulset
                type: string
              name:
                description: Name is the name of the data source
                type: string
              type:
                description: Type is the type of the data source
                type: string
            required:
            - dataSourceList
            - exporterRef
            - name
            - type
            type: object
          status:
            description: CollectorDataSourceStatus defines the observed state of CollectorDataSource
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
