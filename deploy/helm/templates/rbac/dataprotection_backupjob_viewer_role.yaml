# permissions for end users to view backupjobs.
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ include "opendbaas-core.fullname" . }}-backupjob-viewer-role
rules:
- apiGroups:
  - dataprotection.infracreate.com
  resources:
  - backupjobs
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - dataprotection.infracreate.com
  resources:
  - backupjobs/status
  verbs:
  - get
