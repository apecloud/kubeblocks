# permissions for end users to view appversions.
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ include "kubeblocks.fullname" . }}-appversion-viewer-role
  labels:
    {{- include "kubeblocks.labels" . | nindent 4 }}
rules:
- apiGroups:
  - dbaas.kubeblocks.io
  resources:
  - appversions
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - dbaas.kubeblocks.io
  resources:
  - appversions/status
  verbs:
  - get
