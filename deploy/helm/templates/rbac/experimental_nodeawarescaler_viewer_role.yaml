# permissions for end users to view nodeawarescalers.
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    {{- include "kubeblocks.labels" . | nindent 4 }}
  name: {{ include "kubeblocks.fullname" . }}-nodeawarescaler-viewer-role
rules:
- apiGroups:
  - experimental.kubeblocks.io
  resources:
  - nodeawarescalers
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - experimental.kubeblocks.io
  resources:
  - nodeawarescalers/status
  verbs:
  - get
