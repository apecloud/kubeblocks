# permissions for end users to view opsrequests.
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ include "opendbaas-core.fullname" . }}-opsrequest-viewer-role
rules:
- apiGroups:
  - dbaas.infracreate.com
  resources:
  - opsrequests
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - dbaas.infracreate.com
  resources:
  - opsrequests/status
  verbs:
  - get
