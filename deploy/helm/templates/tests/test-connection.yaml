apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "opendbaas-core.fullname" . }}-test-connection"
  labels:
    {{- include "opendbaas-core.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "opendbaas-core.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
