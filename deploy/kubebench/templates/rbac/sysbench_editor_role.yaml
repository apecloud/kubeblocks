# permissions for end users to edit sysbenches.
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ include "kubebench.fullname" . }}-sysbench-editor-role
  labels:
    {{- include "kubebench.labels" . | nindent 4 }}
rules:
- apiGroups:
  - benchmark.kubebench.io
  resources:
  - sysbenches
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - benchmark.kubebench.io
  resources:
  - sysbenches/status
  verbs:
  - get
