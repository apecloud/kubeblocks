apiVersion: v1
kind: ConfigMap
metadata:
  name: mariadb-config-template
  labels:
    {{- include "mariadb.labels" . | nindent 4 }}
data:
  my.cnf: |-
    {{- .Files.Get "config/mariadb-config.tpl" | nindent 4 }}
