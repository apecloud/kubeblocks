apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: kb-role-{{ .Release.Name }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{ include "mongodb-cluster.labels" . | nindent 4 }}
rules:
  - apiGroups:
      - ""
    resources:
      - events
    verbs:
      - create
