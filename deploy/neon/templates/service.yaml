apiVersion: v1
kind: Service
metadata:
  name: {{ include "neon.fullname" . }}
  labels:
    {{- include "neon.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "neon.selectorLabels" . | nindent 4 }}
