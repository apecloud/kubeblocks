apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "neon.fullname" . }}-test-connection"
  labels:
    {{- include "neon.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "neon.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
