apiVersion: apps.kubeblocks.io/v1alpha1
kind: ConfigConstraint
metadata:
  name: opensearch-config-constraint
  labels:
    {{- include "opensearch.labels" . | nindent 4 }}
spec:
  formatterConfig:
    format: yaml