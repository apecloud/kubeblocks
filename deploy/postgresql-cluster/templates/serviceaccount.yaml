{{- if .Values.serviceAccount.enabled }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: kb-sa-{{ .Release.Name }}
  labels:
    {{ include "postgresqlcluster.labels" . | nindent 4 }}
{{- end }}