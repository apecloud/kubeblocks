apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "pulsar.name" . }}-bookies-conf-tpl
  labels:
    {{- include "pulsar.labels" . | nindent 4 }}
data:
  conf: |-
    {{- .Files.Get "config/bookies-config.tpl" | nindent 4 }}

