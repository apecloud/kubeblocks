apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "pulsar.name" . }}-broker-env-tpl
  labels:
    {{- include "pulsar.labels" . | nindent 4 }}
data:
  conf: |-
    {{- .Files.Get "config/broker-env.tpl" | nindent 4 }}
---
  apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "pulsar.name" . }}-broker-conf-tpl
  labels:
    {{- include "pulsar.labels" . | nindent 4 }}
data:
  broker.conf: |-
    {{- .Files.Get "config/broker-config.tpl" | nindent 4 }}