apiVersion: apps.kubeblocks.io/v1alpha1
kind: ConfigConstraint
metadata:
  name: pulsar-config-constraints
  labels:
    {{- include "pulsar.labels" . | nindent 4 }}
spec:
  configurationSchema:
    cue: ""

  # mysql configuration file format
  formatterConfig:
    format: yaml