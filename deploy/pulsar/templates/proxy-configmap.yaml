apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "pulsar.name" . }}-proxy-conf-tpl
  labels:
    {{- include "pulsar.labels" . | nindent 4 }}
data:
  conf: |-
    {{- .Files.Get "config/proxy-config.tpl" | nindent 4 }}

