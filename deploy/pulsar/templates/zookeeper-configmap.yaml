apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "pulsar.name" . }}-zookeeper-conf-tpl
  labels:
    {{- include "pulsar.labels" . | nindent 4 }}
data:
  conf: |-
    {{- .Files.Get "config/zookeeper-config.tpl" | nindent 4 }}

