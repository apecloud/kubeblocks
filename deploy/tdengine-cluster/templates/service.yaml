apiVersion: v1
kind: Service
metadata:
  name: {{ include "tdengine-cluster.fullname" . }}
  labels:
    {{- include "tdengine-cluster.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tdengine-cluster.selectorLabels" . | nindent 4 }}
