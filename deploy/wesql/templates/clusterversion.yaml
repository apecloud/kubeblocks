apiVersion: dbaas.kubeblocks.io/v1alpha1
kind: ClusterVersion
metadata:
  name: wesql-{{ default .Chart.AppVersion .Values.clusterVersionOverride }}
  labels:
    {{- include "wesqlapp.labels" . | nindent 4 }}
spec:
  clusterDefinitionRef: apecloud-wesql
  components:
  - type: replicasets
    podSpec:
      containers:
      - name: mysql
        image: {{ .Values.image.repository }}:{{ .Values.image.tag }}
        imagePullPolicy: {{ default .Values.image.pullPolicy "IfNotPresent" }}
