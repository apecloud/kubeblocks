# Standard Kubernetes Kind declaration. Required.
apiVersion: dataprotection.kubeblocks.io/v1alpha1
# Standard Kubernetes Kind declaration. Required.
kind: Backup
# Standard Kubernetes metadata. Required.
metadata:
  # Backup name. Maybe any valid Kubernetes object name. Required.
  name: backup-demo

  labels:
    dataprotection.kubeblocks.io/backup-type: snapshot
    # if type is incremental, parentName is required.
    #dataprotection.kubeblocks.io/parent-backup-name: null
    # reference db cluster name
    clusters.apps.kubeblocks.io/name: wesql-cluster
    backuppolicies.dataprotection.kubeblocks.io/name: backup-policy-demo
    # backup index begin to 0 (full) for incremental.
    dataprotection.kubeblocks.io/backup-index: "0"

# Parameters about the backup. Required.
spec:
  backupPolicyName: backup-policy-demo

  # Backup Type. full or incremental or snapshot. if unset, default is full.
  backupType: snapshot

  # ttl value  from backup policy optional, not clean if unset.
  ttl: 168h0m0s
