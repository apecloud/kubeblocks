apiVersion: v1
kind: ConfigMap
metadata:
  name: mysql-secondary-config-8.0.31
data:
  mysql.cnf: |-
    # 从节点MySQL的配置文件
    [mysqld]
    super-read-only
    gtid_mode=ON
    enforce-gtid-consistency=true
