apiVersion: apps.kubeblocks.io/v1alpha1
kind: OpsRequest
metadata:
  name: openldap-stop
  namespace: default
spec:
  # Specifies the name of the Cluster resource that this operation is targeting.
  clusterName: openldap-cluster
  type: Stop
