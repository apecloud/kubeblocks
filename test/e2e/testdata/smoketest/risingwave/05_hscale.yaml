apiVersion: apps.kubeblocks.io/v1alpha1
kind: OpsRequest
metadata:
  generateName: risingwave-cluster-hscale-
spec:
  clusterRef: risingwave-cluster
  type: HorizontalScaling
  horizontalScaling:
    - componentName: frontend
      replicas: 3
    - componentName: meta
        replicas: 3