apiVersion: dbaas.kubeblocks.io/v1alpha1
kind:       ClusterVersion
metadata:
  name:  %s
spec:
  clusterDefinitionRef: %s
  componentVersions:
  - componentDefRef: consensus
    podSpec:
      containers:
      - name: mysql
        image: %s
  - podSpec:
      containers:
      - image: %s
        imagePullPolicy: IfNotPresent
        name: busybox
    componentDefRef: stateless
  - podSpec:
      containers:
      - image: %s
        imagePullPolicy: IfNotPresent
        name: mysql
    componentDefRef: stateful