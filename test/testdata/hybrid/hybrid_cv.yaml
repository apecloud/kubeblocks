apiVersion: dbaas.kubeblocks.io/v1alpha1
kind: ClusterVersion
metadata:
  name: %s
spec:
  clusterDefinitionRef: %s
  components:
  - type: consensus
    podSpec:
      containers:
      - name: mysql
        image: %s
  - podSpec:
      containers:
      - image: %s
        imagePullPolicy: IfNotPresent
        name: busybox
    type: stateless
  - podSpec:
      containers:
      - image: %s
        imagePullPolicy: IfNotPresent
        name: mysql
    type: stateful